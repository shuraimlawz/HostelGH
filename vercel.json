{
    "version": 2,
    "builds": [
        {
            "src": "web/package.json",
            "use": "@vercel/next"
        }
    ],
    "buildCommand": "npm --prefix web run build",
    "outputDirectory": ".next"
}